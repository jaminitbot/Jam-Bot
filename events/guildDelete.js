module.exports = {
	register(guild, db) {
		// db.run('DROP TABLE "' + guild.id + '"', (err) => { // Delete the table for the guild
		// 	if (err) {
		// 		return console.error(err.message)
		// 	}
		// })
	}
}
