{
    "settings": {
        "owner": "ID",
        "token": "TOKEN",
        "database": {
            "databaseType": "sqlite",
            "databaseLocation": "database.db"
        }
    },
    "defaults": {
        "prefix": "!"
    }
}