{
    "settings": {
        "ownerid": "ID",
        "ownername": "NAME",
        "token": "TOKEN",
        "database": {
            "databaseType": "sqlite",
            "databaseLocation": "database.db"
        }
    },
    "defaults": {
        "prefix": "!"
    }
}