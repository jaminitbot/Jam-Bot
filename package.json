{
	"devDependencies": {
		"turbo": "^1.4.3"
	},
	"scripts": {
		"build": "turbo run build",
		"start": "cd packages/jam-bot && pnpm start",
		"lint": "turbo run lint",
		"deploy": "pnpm --filter=@jaminitbot/jam-bot --prod deploy output"
	}
}